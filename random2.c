#include <stdio.h>

int main() {
  int a = 98765,b = 43210,c, d;
  c = a-b;
  d = a+b;
   // printf("DIFFERENCE BETWEEN A AND B IS: %d", c);
    printf("DIFFERENCE BETWEEN A AND B IS: %d\n", a-b);
    printf("sum of A AND B IS: %d\n", d);
  return 0;
}
